#include "position.h"
#include "evaluation.h"
#include <chrono>
#include <tuple>

using namepsace std;
using namepsace std::chrono;

pair<Position, evaluation> search(Position position, system_clock::time_point limit)
{
	//TODO: tudo

	do
	{

	}
	while (system_clock::now() <= limit);

	return
}

/*evaluation search(Position position, int depth)
{
	if (depth == 0)
		return 
}*/